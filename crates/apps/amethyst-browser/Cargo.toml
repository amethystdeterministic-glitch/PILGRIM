[package]
name = "amethyst-browser"
version = "0.1.0"
edition = "2021"

[dependencies]
ghostpass-core = { path = "../ghostpass-core" }
zyte-core      = { path = "../zyte-core" }

serde      = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

base64 = "0.22"
